import React from 'react';
import { Bell, Search, User } from 'lucide-react';
import { useLocation } from 'react-router-dom';

const Navbar = () => {
  return (
    <nav className="bg-white border-b border-gray-100 p-4 px-8 flex justify-center items-center sticky top-0 z-10 h-16">
      {/* Minimal clean header - no elements, just space */}
    </nav>
  );
};

export default Navbar;
